apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: supply-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7
          envFrom:
            - secretRef:
                name: app-secrets
          command: ["redis-server", "--requirepass", "$(spring_data_redis_password)"]
          ports:
            - containerPort: 6379
          livenessProbe:
            exec:
              command: ["redis-cli", "-a", "$(spring_data_redis_password)", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["redis-cli", "-a", "$(spring_data_redis_password)", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              memory: 256Mi
              cpu: 500m
            requests:
              memory: 128Mi
              cpu: 100m